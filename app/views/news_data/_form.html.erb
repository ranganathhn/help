<%= form_for(news_datum) do |f| %>
  <% if news_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_datum.errors.count, "error") %> prohibited this news_datum from being saved:</h2>

      <ul>
      <% news_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :headlines %>
    <%= f.text_area :headlines %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :category_id %>
    <%= f.text_field :category_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
